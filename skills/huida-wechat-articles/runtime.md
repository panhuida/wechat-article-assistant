# 本机运行位置

此文件由 `scripts/install-codex-skill.ps1` 在 `~/.agents/skills/huida-wechat-articles` 中生成，记录当前机器的项目启动器绝对路径和应用数据目录。用户 PATH 需手动加入项目的 `scripts` 目录。
仓库中的技能尚未安装时，先在项目根目录运行 `uv sync`，再运行安装脚本。不要从其他任务的工作目录推测数据位置。
